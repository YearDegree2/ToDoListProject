{% extends 'ToDoListBundle:Default:task.base.html.twig' %}

{% block content %}
    <label for="message"><h3><span class="label label-default">Update your Task</span></h3></label><br/>

    {{ form_start(form) }}
    {{ form_row(form.name) }}
    <br/>
    {{ form_row(form.Update) }}
    <br/>
    {{ form_rest(form) }}
    {{ form_end(form) }}
{% endblock %}
